//quizMaster requires arguments to update jSON file. 
var myQuiz = quizMaster;
myQuiz.init({
	theme: "theme-two",
	backButton: true,
	restartButton: true,
	questionCount: false,
	font: "trebuchet",
	fadeIn: true
});