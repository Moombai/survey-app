//quizMaster requires arguments to update jSON file. 
var myQuiz = quizMaster;
myQuiz.init({
	theme: "theme-two",
	backButton: true,
	restartButton: true,
	questionCount: true,
	font: "trebuchet",
	fadeIn: true
});